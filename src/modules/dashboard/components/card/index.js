import React from 'react';
import { Card } from '../../../../components';

class CardComponent extends React.PureComponent{
    render() {
        return(
            <Card />
        )
    }
}

export default Card;